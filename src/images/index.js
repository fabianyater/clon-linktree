import { DarkIcon, DeleteIcon, LightIcon, PlusIcon, ShareIcon } from "./SvgIcon";

const icons = {
  lightTheme: LightIcon,
  darkTheme: DarkIcon,
  plus: PlusIcon,
  delete: DeleteIcon,
  share: ShareIcon
}

export default icons;